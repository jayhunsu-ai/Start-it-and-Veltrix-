# The URL of your deployed Render backend, no trailing slash.
# Set this in your hosting provider's dashboard (Vercel/Netlify/Render
# static site env vars) — never commit a real value here.
VITE_API_BASE=https://your-backend.onrender.com
